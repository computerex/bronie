def talk_to_user(message):
    """
    Communicate with the user by returning a message.
    This function simply returns the message that will be shown to the user.
    
    Args:
        message (str): Message to send to the user
        
    Returns:
        str: The same message, to be displayed to the user
    """
    return message
